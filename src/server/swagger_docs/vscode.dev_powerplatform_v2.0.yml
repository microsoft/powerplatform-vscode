openapi: "3.0.2"
info:
  title: VSCode.dev API for PowerPlatform
  version: "2.0"
servers:
  - url: https://vscode.dev/
tags:
  - name: portal
    description: Everything about editing portal in vscode
  - name: default
    description: Everything about editing PowerPlatform default app in vscode
paths:
  /power/portal/webpages/{webpageId}:
    get:
      tags:
        - portal
      description: "Edit a specific webpage"
      parameters:
        - name: "webpageId"
          in: "path"
          description: "ID of the webpage"
          required: true
          schema:
            type: string
            format: guid
        - name: "orgUrl"
          in: "query"
          description: "Dataverse OrgUrl"
          required: true
          schema:
            type: string
          example: https://contoso.crm.dynamics.com
        - name: "dataSource"
          in: "query"
          description: "Location where the portal data is saved"
          required: true
          schema:
            type: string
          example: dataverse
        - name: "schema"
          in: "query"
          description: "Schema file name of the app"
          required: true
          schema:
            type: string
          examples:
            portal old schema:
              value: PortalSchemaV1
              summary: old portal schema
            portal new schema:
              value: PortalSchemaV2
              summary: new portal schema
        - name: "websiteId"
          in: "query"
          description: "ID of the website"
          required: true
          schema:
            type: string
            format: guid
        - name: "websiteName"
          in: "query"
          description: "Name of the portal you want to edit"
          required: false
          schema:
            type: string
            default: site
        - name: "referrerSessionId"
          in: "query"
          description: "Referrer SessionId - this serves as the correlationId between Referrer and VSCode extension"
          required: false
          schema:
            type: string
        - name: "referrer"
          in: "query"
          description: "Name of the referrer from where vscode is launched. This is used only for analytics."
          required: false
          schema:
            type: string
          example: PowerPagesStudio
      responses:
        "200":
          description: OK
  /power/portal/webpages:
    get:
      tags:
        - portal
      description: "Edit all webpages"
      parameters:
        - name: "orgUrl"
          in: "query"
          description: "Dataverse OrgUrl"
          required: true
          schema:
            type: string
          example: https://contoso.crm.dynamics.com
        - name: "dataSource"
          in: "query"
          description: "Location where the portal data is saved"
          required: true
          schema:
            type: string
          examples:
            dataverse:
              value: dataverse
              summary: if the data is saved in dataverse
            github:
              value: github
              summary: if the data is saved in github
        - name: "schema"
          in: "query"
          description: "Schema file name of the app"
          required: true
          schema:
            type: string
          examples:
            portal old schema:
              value: PortalSchemaV1
              summary: old portal schema
            portal new schema:
              value: PortalSchemaV2
              summary: new portal schema
        - name: "websiteId"
          in: "query"
          description: "ID of the website"
          required: true
          schema:
            type: string
            format: guid
        - name: "websiteName"
          in: "query"
          description: "Name of the portal you want to edit"
          required: false
          schema:
            type: string
            default: site
        - name: "clientSessionId"
          in: "query"
          description: "Studio SessionId - this serves as the correlationId between Studio and VSCode extension"
          required: false
          schema:
            type: string
        - name: "client"
          in: "query"
          description: "Name of the client from where vscode is launched. This is used only for analytics."
          required: false
          schema:
            type: string
          example: PowerPagesStudio
      responses:
        "200":
          description: OK
  /power/portal/webfiles/{webfileId}:
    get:
      tags:
        - portal
      description: "Edit a specific webfile"
      parameters:
        - name: "webfileId"
          in: "path"
          description: "ID of the webfile"
          required: true
          schema:
            type: string
            format: guid
        - name: "orgUrl"
          in: "query"
          description: "Dataverse OrgUrl"
          required: true
          schema:
            type: string
          example: https://contoso.crm.dynamics.com
        - name: "dataSource"
          in: "query"
          description: "Location where the portal data is saved"
          required: true
          schema:
            type: string
          examples:
            dataverse:
              value: dataverse
              summary: if the data is saved in dataverse
            github:
              value: github
              summary: if the data is saved in github
        - name: "schema"
          in: "query"
          description: "Schema file name of the app"
          required: true
          schema:
            type: string
          examples:
            portal old schema:
              value: PortalSchemaV1
              summary: old portal schema
            portal new schema:
              value: PortalSchemaV2
              summary: new portal schema
        - name: "websiteId"
          in: "query"
          description: "ID of the website"
          required: true
          schema:
            type: string
            format: guid
        - name: "websiteName"
          in: "query"
          description: "Name of the portal you want to edit"
          required: false
          schema:
            type: string
            default: site
        - name: "clientSessionId"
          in: "query"
          description: "Studio SessionId - this serves as the correlationId between Studio and VSCode extension"
          required: false
          schema:
            type: string
        - name: "client"
          in: "query"
          description: "Name of the client from where vscode is launched. This is used only for analytics."
          required: false
          schema:
            type: string
          example: PowerPagesStudio
      responses:
        "200":
          description: OK
  /power/portal/webfiles:
    get:
      tags:
        - portal
      description: "Edit all webfiles"
      parameters:
        - name: "orgUrl"
          in: "query"
          description: "Dataverse OrgUrl"
          required: true
          schema:
            type: string
          example: https://contoso.crm.dynamics.com
        - name: "dataSource"
          in: "query"
          description: "Location where the portal data is saved"
          required: true
          schema:
            type: string
          examples:
            dataverse:
              value: dataverse
              summary: if the data is saved in dataverse
            github:
              value: github
              summary: if the data is saved in github
        - name: "schema"
          in: "query"
          description: "Schema file name of the app"
          required: true
          schema:
            type: string
          examples:
            portal old schema:
              value: PortalSchemaV1
              summary: old portal schema
            portal new schema:
              value: PortalSchemaV2
              summary: new portal schema
        - name: "websiteId"
          in: "query"
          description: "ID of the website"
          required: true
          schema:
            type: string
            format: guid
        - name: "websiteName"
          in: "query"
          description: "Name of the portal you want to edit"
          required: false
          schema:
            type: string
            default: site
        - name: "clientSessionId"
          in: "query"
          description: "Studio SessionId - this serves as the correlationId between Studio and VSCode extension"
          required: false
          schema:
            type: string
        - name: "client"
          in: "query"
          description: "Name of the client from where vscode is launched. This is used only for analytics."
          required: false
          schema:
            type: string
          example: PowerPagesStudio
      responses:
        "200":
          description: OK
  /power/portal/webtemplates/{webtemplateId}:
    get:
      tags:
        - portal
      description: "Edit a specific webtemplate"
      parameters:
        - name: "webtemplateId"
          in: "path"
          description: "ID of the webtemplate"
          required: true
          schema:
            type: string
            format: guid
        - name: "orgUrl"
          in: "query"
          description: "Dataverse OrgUrl"
          required: true
          schema:
            type: string
          example: https://contoso.crm.dynamics.com
        - name: "dataSource"
          in: "query"
          description: "Location where the portal data is saved"
          required: true
          schema:
            type: string
          examples:
            dataverse:
              value: dataverse
              summary: if the data is saved in dataverse
            github:
              value: github
              summary: if the data is saved in github
        - name: "schema"
          in: "query"
          description: "Schema file name of the app"
          required: true
          schema:
            type: string
          examples:
            portal old schema:
              value: PortalSchemaV1
              summary: old portal schema
            portal new schema:
              value: PortalSchemaV2
              summary: new portal schema
        - name: "websiteId"
          in: "query"
          description: "ID of the website"
          required: true
          schema:
            type: string
            format: guid
        - name: "websiteName"
          in: "query"
          description: "Name of the portal you want to edit"
          required: false
          schema:
            type: string
            default: site
        - name: "clientSessionId"
          in: "query"
          description: "Studio SessionId - this serves as the correlationId between Studio and VSCode extension"
          required: false
          schema:
            type: string
        - name: "client"
          in: "query"
          description: "Name of the client from where vscode is launched. This is used only for analytics."
          required: false
          schema:
            type: string
          example: PowerPagesStudio
      responses:
        "200":
          description: OK
  /power/portal/webtemplates:
    get:
      tags:
        - portal
      description: "Edit all webtemplates"
      parameters:
        - name: "orgUrl"
          in: "query"
          description: "Dataverse OrgUrl"
          required: true
          schema:
            type: string
          example: https://contoso.crm.dynamics.com
        - name: "dataSource"
          in: "query"
          description: "Location where the portal data is saved"
          required: true
          schema:
            type: string
          examples:
            dataverse:
              value: dataverse
              summary: if the data is saved in dataverse
            github:
              value: github
              summary: if the data is saved in github
        - name: "schema"
          in: "query"
          description: "Schema file name of the app"
          required: true
          schema:
            type: string
          examples:
            portal old schema:
              value: PortalSchemaV1
              summary: old portal schema
            portal new schema:
              value: PortalSchemaV2
              summary: new portal schema
        - name: "websiteId"
          in: "query"
          description: "ID of the website"
          required: true
          schema:
            type: string
            format: guid
        - name: "websiteName"
          in: "query"
          description: "Name of the portal you want to edit"
          required: false
          schema:
            type: string
            default: site
        - name: "clientSessionId"
          in: "query"
          description: "Studio SessionId - this serves as the correlationId between Studio and VSCode extension"
          required: false
          schema:
            type: string
        - name: "client"
          in: "query"
          description: "Name of the client from where vscode is launched. This is used only for analytics."
          required: false
          schema:
            type: string
          example: PowerPagesStudio
      responses:
        "200":
          description: OK
  /power/portal/pagetemplates/{pagetemplateId}:
    get:
      tags:
        - portal
      description: "Edit a specific pagetemplate"
      parameters:
        - name: "pagetemplateId"
          in: "path"
          description: "ID of the pagetemplate"
          required: true
          schema:
            type: string
            format: guid
        - name: "orgUrl"
          in: "query"
          description: "Dataverse OrgUrl"
          required: true
          schema:
            type: string
          example: https://contoso.crm.dynamics.com
        - name: "dataSource"
          in: "query"
          description: "Location where the portal data is saved"
          required: true
          schema:
            type: string
          examples:
            dataverse:
              value: dataverse
              summary: if the data is saved in dataverse
            github:
              value: github
              summary: if the data is saved in github
        - name: "schema"
          in: "query"
          description: "Schema file name of the app"
          required: true
          schema:
            type: string
          examples:
            portal old schema:
              value: PortalSchemaV1
              summary: old portal schema
            portal new schema:
              value: PortalSchemaV2
              summary: new portal schema
        - name: "websiteId"
          in: "query"
          description: "ID of the website"
          required: true
          schema:
            type: string
            format: guid
        - name: "websiteName"
          in: "query"
          description: "Name of the portal you want to edit"
          required: false
          schema:
            type: string
            default: site
        - name: "clientSessionId"
          in: "query"
          description: "Studio SessionId - this serves as the correlationId between Studio and VSCode extension"
          required: false
          schema:
            type: string
        - name: "client"
          in: "query"
          description: "Name of the client from where vscode is launched. This is used only for analytics."
          required: false
          schema:
            type: string
          example: PowerPagesStudio
      responses:
        "200":
          description: OK
  /power/portal/pagetemplates:
    get:
      tags:
        - portal
      description: "Edit all pagetemplates"
      parameters:
        - name: "orgUrl"
          in: "query"
          description: "Dataverse OrgUrl"
          required: true
          schema:
            type: string
          example: https://contoso.crm.dynamics.com
        - name: "dataSource"
          in: "query"
          description: "Location where the portal data is saved"
          required: true
          schema:
            type: string
          examples:
            dataverse:
              value: dataverse
              summary: if the data is saved in dataverse
            github:
              value: github
              summary: if the data is saved in github
        - name: "schema"
          in: "query"
          description: "Schema file name of the app"
          required: true
          schema:
            type: string
          examples:
            portal old schema:
              value: PortalSchemaV1
              summary: old portal schema
            portal new schema:
              value: PortalSchemaV2
              summary: new portal schema
        - name: "websiteId"
          in: "query"
          description: "ID of the website"
          required: true
          schema:
            type: string
            format: guid
        - name: "websiteName"
          in: "query"
          description: "Name of the portal you want to edit"
          required: false
          schema:
            type: string
            default: site
        - name: "clientSessionId"
          in: "query"
          description: "Studio SessionId - this serves as the correlationId between Studio and VSCode extension"
          required: false
          schema:
            type: string
        - name: "client"
          in: "query"
          description: "Name of the client from where vscode is launched. This is used only for analytics."
          required: false
          schema:
            type: string
          example: PowerPagesStudio
      responses:
        "200":
          description: OK
  /power/portal/contentsnippets/{contentsnippetId}:
    get:
      tags:
        - portal
      description: "Edit a specific contentsnippet"
      parameters:
        - name: "contentsnippetId"
          in: "path"
          description: "ID of the contentsnippet"
          required: true
          schema:
            type: string
            format: guid
        - name: "orgUrl"
          in: "query"
          description: "Dataverse OrgUrl"
          required: true
          schema:
            type: string
          example: https://contoso.crm.dynamics.com
        - name: "dataSource"
          in: "query"
          description: "Location where the portal data is saved"
          required: true
          schema:
            type: string
          examples:
            dataverse:
              value: dataverse
              summary: if the data is saved in dataverse
            github:
              value: github
              summary: if the data is saved in github
        - name: "schema"
          in: "query"
          description: "Schema file name of the app"
          required: true
          schema:
            type: string
          examples:
            portal old schema:
              value: PortalSchemaV1
              summary: old portal schema
            portal new schema:
              value: PortalSchemaV2
              summary: new portal schema
        - name: "websiteId"
          in: "query"
          description: "ID of the website"
          required: true
          schema:
            type: string
            format: guid
        - name: "websiteName"
          in: "query"
          description: "Name of the portal you want to edit"
          required: false
          schema:
            type: string
            default: site
        - name: "clientSessionId"
          in: "query"
          description: "Studio SessionId - this serves as the correlationId between Studio and VSCode extension"
          required: false
          schema:
            type: string
        - name: "client"
          in: "query"
          description: "Name of the client from where vscode is launched. This is used only for analytics."
          required: false
          schema:
            type: string
          example: PowerPagesStudio
      responses:
        "200":
          description: OK
  /power/portal/contentsnippets:
    get:
      tags:
        - portal
      description: "Edit all contentsnippets"
      parameters:
        - name: "orgUrl"
          in: "query"
          description: "Dataverse OrgUrl"
          required: true
          schema:
            type: string
          example: https://contoso.crm.dynamics.com
        - name: "dataSource"
          in: "query"
          description: "Location where the portal data is saved"
          required: true
          schema:
            type: string
          examples:
            dataverse:
              value: dataverse
              summary: if the data is saved in dataverse
            github:
              value: github
              summary: if the data is saved in github
        - name: "schema"
          in: "query"
          description: "Schema file name of the app"
          required: true
          schema:
            type: string
          examples:
            portal old schema:
              value: PortalSchemaV1
              summary: old portal schema
            portal new schema:
              value: PortalSchemaV2
              summary: new portal schema
        - name: "websiteId"
          in: "query"
          description: "ID of the website"
          required: true
          schema:
            type: string
            format: guid
        - name: "websiteName"
          in: "query"
          description: "Name of the portal you want to edit"
          required: false
          schema:
            type: string
            default: site
        - name: "clientSessionId"
          in: "query"
          description: "Studio SessionId - this serves as the correlationId between Studio and VSCode extension"
          required: false
          schema:
            type: string
        - name: "client"
          in: "query"
          description: "Name of the client from where vscode is launched. This is used only for analytics."
          required: false
          schema:
            type: string
          example: PowerPagesStudio
      responses:
        "200":
          description: OK
  /power/portal/lists/{listId}:
    get:
      tags:
        - portal
      description: "Edit a specific entity List"
      parameters:
        - name: "listId"
          in: "path"
          description: "ID of the List"
          required: true
          schema:
            type: string
            format: guid
        - name: "orgUrl"
          in: "query"
          description: "Dataverse OrgUrl"
          required: true
          schema:
            type: string
          example: https://contoso.crm.dynamics.com
        - name: "dataSource"
          in: "query"
          description: "Location where the portal data is saved"
          required: true
          schema:
            type: string
          examples:
            dataverse:
              value: dataverse
              summary: if the data is saved in dataverse
            github:
              value: github
              summary: if the data is saved in github
        - name: "schema"
          in: "query"
          description: "Schema file name of the app"
          required: true
          schema:
            type: string
          examples:
            portal old schema:
              value: PortalSchemaV1
              summary: old portal schema
            portal new schema:
              value: PortalSchemaV2
              summary: new portal schema
        - name: "websiteId"
          in: "query"
          description: "ID of the website"
          required: true
          schema:
            type: string
            format: guid
        - name: "websiteName"
          in: "query"
          description: "Name of the portal you want to edit"
          required: false
          schema:
            type: string
            default: site
        - name: "clientSessionId"
          in: "query"
          description: "Studio SessionId - this serves as the correlationId between Studio and VSCode extension"
          required: false
          schema:
            type: string
        - name: "client"
          in: "query"
          description: "Name of the client from where vscode is launched. This is used only for analytics."
          required: false
          schema:
            type: string
          example: PowerPagesStudio
      responses:
        "200":
          description: OK
  /power/portal/lists:
    get:
      tags:
        - portal
      description: "Edit all Lists"
      parameters:
        - name: "orgUrl"
          in: "query"
          description: "Dataverse OrgUrl"
          required: true
          schema:
            type: string
          example: https://contoso.crm.dynamics.com
        - name: "dataSource"
          in: "query"
          description: "Location where the portal data is saved"
          required: true
          schema:
            type: string
          examples:
            dataverse:
              value: dataverse
              summary: if the data is saved in dataverse
            github:
              value: github
              summary: if the data is saved in github
        - name: "schema"
          in: "query"
          description: "Schema file name of the app"
          required: true
          schema:
            type: string
          examples:
            portal old schema:
              value: PortalSchemaV1
              summary: old portal schema
            portal new schema:
              value: PortalSchemaV2
              summary: new portal schema
        - name: "websiteId"
          in: "query"
          description: "ID of the website"
          required: true
          schema:
            type: string
            format: guid
        - name: "websiteName"
          in: "query"
          description: "Name of the portal you want to edit"
          required: false
          schema:
            type: string
            default: site
        - name: "clientSessionId"
          in: "query"
          description: "Studio SessionId - this serves as the correlationId between Studio and VSCode extension"
          required: false
          schema:
            type: string
        - name: "client"
          in: "query"
          description: "Name of the client from where vscode is launched. This is used only for analytics."
          required: false
          schema:
            type: string
          example: PowerPagesStudio
      responses:
        "200":
          description: OK
  /power/portal/basicforms/{basicformId}:
    get:
      tags:
        - portal
      description: "Edit a specific basicform"
      parameters:
        - name: "basicformId"
          in: "path"
          description: "ID of the basicform"
          required: true
          schema:
            type: string
            format: guid
        - name: "orgUrl"
          in: "query"
          description: "Dataverse OrgUrl"
          required: true
          schema:
            type: string
          example: https://contoso.crm.dynamics.com
        - name: "dataSource"
          in: "query"
          description: "Location where the portal data is saved"
          required: true
          schema:
            type: string
          examples:
            dataverse:
              value: dataverse
              summary: if the data is saved in dataverse
            github:
              value: github
              summary: if the data is saved in github
        - name: "schema"
          in: "query"
          description: "Schema file name of the app"
          required: true
          schema:
            type: string
          examples:
            portal old schema:
              value: PortalSchemaV1
              summary: old portal schema
            portal new schema:
              value: PortalSchemaV2
              summary: new portal schema
        - name: "websiteId"
          in: "query"
          description: "ID of the website"
          required: true
          schema:
            type: string
            format: guid
        - name: "websiteName"
          in: "query"
          description: "Name of the portal you want to edit"
          required: false
          schema:
            type: string
            default: site
        - name: "clientSessionId"
          in: "query"
          description: "Studio SessionId - this serves as the correlationId between Studio and VSCode extension"
          required: false
          schema:
            type: string
        - name: "client"
          in: "query"
          description: "Name of the client from where vscode is launched. This is used only for analytics."
          required: false
          schema:
            type: string
          example: PowerPagesStudio
      responses:
        "200":
          description: OK
  /power/portal/basicforms:
    get:
      tags:
        - portal
      description: "Edit all basicforms"
      parameters:
        - name: "orgUrl"
          in: "query"
          description: "Dataverse OrgUrl"
          required: true
          schema:
            type: string
          example: https://contoso.crm.dynamics.com
        - name: "dataSource"
          in: "query"
          description: "Location where the portal data is saved"
          required: true
          schema:
            type: string
          examples:
            dataverse:
              value: dataverse
              summary: if the data is saved in dataverse
            github:
              value: github
              summary: if the data is saved in github
        - name: "schema"
          in: "query"
          description: "Schema file name of the app"
          required: true
          schema:
            type: string
          examples:
            portal old schema:
              value: PortalSchemaV1
              summary: old portal schema
            portal new schema:
              value: PortalSchemaV2
              summary: new portal schema
        - name: "websiteId"
          in: "query"
          description: "ID of the website"
          required: true
          schema:
            type: string
            format: guid
        - name: "websiteName"
          in: "query"
          description: "Name of the portal you want to edit"
          required: false
          schema:
            type: string
            default: site
        - name: "clientSessionId"
          in: "query"
          description: "Studio SessionId - this serves as the correlationId between Studio and VSCode extension"
          required: false
          schema:
            type: string
        - name: "client"
          in: "query"
          description: "Name of the client from where vscode is launched. This is used only for analytics."
          required: false
          schema:
            type: string
          example: PowerPagesStudio
      responses:
        "200":
          description: OK
  /power/portal/advancedforms/{advancedformId}:
    get:
      tags:
        - portal
      description: "Edit a specific advancedform"
      parameters:
        - name: "advancedformId"
          in: "path"
          description: "ID of the advancedform"
          required: true
          schema:
            type: string
            format: guid
        - name: "orgUrl"
          in: "query"
          description: "Dataverse OrgUrl"
          required: true
          schema:
            type: string
          example: https://contoso.crm.dynamics.com
        - name: "dataSource"
          in: "query"
          description: "Location where the portal data is saved"
          required: true
          schema:
            type: string
          examples:
            dataverse:
              value: dataverse
              summary: if the data is saved in dataverse
            github:
              value: github
              summary: if the data is saved in github
        - name: "schema"
          in: "query"
          description: "Schema file name of the app"
          required: true
          schema:
            type: string
          examples:
            portal old schema:
              value: PortalSchemaV1
              summary: old portal schema
            portal new schema:
              value: PortalSchemaV2
              summary: new portal schema
        - name: "websiteId"
          in: "query"
          description: "ID of the website"
          required: true
          schema:
            type: string
            format: guid
        - name: "websiteName"
          in: "query"
          description: "Name of the portal you want to edit"
          required: false
          schema:
            type: string
            default: site
        - name: "clientSessionId"
          in: "query"
          description: "Studio SessionId - this serves as the correlationId between Studio and VSCode extension"
          required: false
          schema:
            type: string
        - name: "client"
          in: "query"
          description: "Name of the client from where vscode is launched. This is used only for analytics."
          required: false
          schema:
            type: string
          example: PowerPagesStudio
      responses:
        "200":
          description: OK
  /power/portal/advancedforms:
    get:
      tags:
        - portal
      description: "Edit all advancedforms"
      parameters:
        - name: "orgUrl"
          in: "query"
          description: "Dataverse OrgUrl"
          required: true
          schema:
            type: string
          example: https://contoso.crm.dynamics.com
        - name: "dataSource"
          in: "query"
          description: "Location where the portal data is saved"
          required: true
          schema:
            type: string
          examples:
            dataverse:
              value: dataverse
              summary: if the data is saved in dataverse
            github:
              value: github
              summary: if the data is saved in github
        - name: "schema"
          in: "query"
          description: "Schema file name of the app"
          required: true
          schema:
            type: string
          examples:
            portal old schema:
              value: PortalSchemaV1
              summary: old portal schema
            portal new schema:
              value: PortalSchemaV2
              summary: new portal schema
        - name: "websiteId"
          in: "query"
          description: "ID of the website"
          required: true
          schema:
            type: string
            format: guid
        - name: "websiteName"
          in: "query"
          description: "Name of the portal you want to edit"
          required: false
          schema:
            type: string
            default: site
        - name: "clientSessionId"
          in: "query"
          description: "Studio SessionId - this serves as the correlationId between Studio and VSCode extension"
          required: false
          schema:
            type: string
        - name: "client"
          in: "query"
          description: "Name of the client from where vscode is launched. This is used only for analytics."
          required: false
          schema:
            type: string
          example: PowerPagesStudio
      responses:
        "200":
          description: OK
  /power/portal:
    get:
      tags:
        - portal
      description: "Edit multiple portal entities: webpages, webfiles, webtemplates, pagetemplates and content snippets"
      parameters:
        - name: "orgUrl"
          in: "query"
          description: "Dataverse OrgUrl"
          required: true
          schema:
            type: string
          example: https://contoso.crm.dynamics.com
        - name: "dataSource"
          in: "query"
          description: "Location where the portal data is saved"
          required: true
          schema:
            type: string
          examples:
            dataverse:
              value: dataverse
              summary: if the data is saved in dataverse
            github:
              value: github
              summary: if the data is saved in github
        - name: "schema"
          in: "query"
          description: "Schema file name of the app"
          required: true
          schema:
            type: string
          examples:
            portal old schema:
              value: PortalSchemaV1
              summary: old portal schema
            portal new schema:
              value: PortalSchemaV2
              summary: new portal schema
        - name: "websiteId"
          in: "query"
          description: "ID of the website"
          required: true
          schema:
            type: string
            format: guid
        - name: "websiteName"
          in: "query"
          description: "Name of the portal you want to edit"
          required: false
          schema:
            type: string
          example: site
        - name: "clientSessionId"
          in: "query"
          description: "Studio SessionId - this serves as the correlationId between Studio and VSCode extension"
          required: false
          schema:
            type: string
        - name: "client"
          in: "query"
          description: "Name of the client from where vscode is launched. This is used only for analytics."
          required: false
          schema:
            type: string
          example: PowerPagesStudio
      responses:
        "200":
          description: OK
  /default/{entity}/{entityId}:
    get:
      tags:
        - default
      description: "Edit a specific entity"
      parameters:
        - name: "entity"
          in: "path"
          description: "Plural Name of the entity"
          required: true
          schema:
            type: string
          example: contacts
        - name: "entityId"
          in: "path"
          description: "ID of the entity"
          required: true
          schema:
            type: string
            format: guid
        - name: "orgUrl"
          in: "query"
          description: "Dataverse OrgUrl"
          required: true
          schema:
            type: string
          example: https://contoso.crm.dynamics.com
        - name: "dataSource"
          in: "query"
          description: "Location where the app data is stored"
          required: true
          schema:
            type: string
          example: dataverse
        - name: "columns"
          in: "query"
          style: form
          explode: false
          description: "comma separated list of columns to edit"
          required: true
          schema:
            type: string
          example: address1_addressid, address2_addressid
        - name: "splitColumns"
          in: "query"
          description: "edit in a single file or multiple files"
          required: false
          schema:
            type: boolean
            default: true
        - name: "clientSessionId"
          in: "query"
          description: "Studio SessionId - this serves as the correlationId between Studio and VSCode extension"
          required: false
          schema:
            type: string
        - name: "client"
          in: "query"
          description: "Name of the client from where vscode is launched. This is used only for analytics."
          required: false
          schema:
            type: string
          example: PortalManagementApp
      responses:
        "200":
          description: OK
  /default/{entity}:
    get:
      tags:
        - default
      description: "Edit all records of an entity"
      parameters:
        - name: "entity"
          in: "path"
          description: "Plural Name of the entity"
          required: true
          schema:
            type: string
          example: contacts
        - name: "orgUrl"
          in: "query"
          description: "Dataverse OrgUrl"
          required: true
          schema:
            type: string
          example: https://contoso.crm.dynamics.com
        - name: "dataSource"
          in: "query"
          description: "Location where the app data is stored"
          required: true
          schema:
            type: string
          example: dataverse
        - name: "columns"
          in: "query"
          style: form
          explode: false
          description: "comma separated list of columns to edit"
          required: true
          schema:
            type: string
          example: address1_addressid, address2_addressid
        - name: "splitColumns"
          in: "query"
          description: "edit in a single file or multiple files"
          required: false
          schema:
            type: boolean
            default: true
        - name: "clientSessionId"
          in: "query"
          description: "Studio SessionId - this serves as the correlationId between Studio and VSCode extension"
          required: false
          schema:
            type: string
        - name: "client"
          in: "query"
          description: "Name of the client from where vscode is launched. This is used only for analytics."
          required: false
          schema:
            type: string
          example: PortalManagementApp
      responses:
        "200":
          description: OK
